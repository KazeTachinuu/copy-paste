# Server Configuration (local development only - not used in Vercel)
PORT=3000
NODE_ENV=development

# API Configuration
# Local development: Use localhost
# Production (Vercel): Should be relative path or your domain
VITE_API_URL=http://localhost:3000/api

# CORS Configuration
# Development: All localhost origins are automatically allowed
# Production: Set this to your frontend domain deployed on Vercel
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://copy-paste-epita.vercel.app

# Vercel KV (Redis) Configuration
# These are automatically provided by Vercel when you create a KV store
# For local development, you can create a KV store at https://vercel.com/dashboard/stores
# Then link it to your project and pull the environment variables using:
# vercel env pull .env.local
#
# Required variables (auto-generated by Vercel):
# KV_REST_API_URL=your_kv_rest_api_url
# KV_REST_API_TOKEN=your_kv_rest_api_token
# KV_REST_API_READ_ONLY_TOKEN=your_kv_rest_api_read_only_token
