# Server Configuration (local development only - not used in Vercel)
PORT=3000
NODE_ENV=development

# API Configuration
# Local development: Use localhost
# Production (Vercel): Set this to your production domain
VITE_API_URL=http://localhost:3000/api

# CORS Configuration
# Development: All localhost origins are automatically allowed
# Production: Set this to your frontend domain(s) deployed on Vercel
# Comma-separated list of allowed origins
# Example: https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app
